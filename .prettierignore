dist/
.next/
node_modules/
public/sqlite/
pnpm-lock.yaml

# Documentation is hand-wrapped prose; reformatting it produces churn that
# obscures real changes in review.
*.md

# Generated data artifacts.
pipeline/pub/

# The service worker template carries `__PRECACHE__` where an array literal
# goes, so it is not valid JavaScript until `scripts/build-sw.mjs` fills it in.
scripts/sw.template.js
